<h1><%= t('ingredients.catalog')%></h1>

<table>
  <tr>
    <th><%= t('drinks.title')%></th>
    <th><%= t('drinks.description')%></th>
    <th><%= t('drinks.amount')%></th>
    <th><%= t('drinks.price')%></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ingredients.each do |ingredient| %>
  <tr>
    <td><%= ingredient.title %></td>
    <td><%= ingredient.price %></td>
    <td><%= ingredient.amount %></td>
    <td><%= link_to t('show'), ingredient %></td>
    <td><%= link_to t('edit'), edit_ingredient_path(ingredient) %></td>
    <td><%= link_to t('destroy'), ingredient, confirm: t('confirm'), method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('drinks.add_ingredient'), new_ingredient_path %>
